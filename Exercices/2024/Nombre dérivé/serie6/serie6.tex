% SOLUTION SWITCH
\newif\ifsolutions
				\solutionstrue
				\solutionsfalse
				
\input{../preamble.tex}


\AdvanceDate[5]

\begin{document}
\pagestyle{fancy}
\fancyhead[L]{Première}
\fancyhead[C]{\textbf{Exercices de retenue : étude de variations \ifsolutions -- Solutions \fi}}
\fancyhead[R]{\today}


\exe{
	On considère la fonction $f$ du troisième degré définie sur le domaine $\D=\left[-1 ; 2\right]$ :
		\[ f(x) = 2x^3 - \dfrac12 x^2 -2x + \dfrac25. \]
		
	\begin{enumerate}
		\item Donner $f'$, la fonction dérivée de $f$.
		\item Montrer que $f'(x) = (2x+1)(3x-2)$ en développant soigneusement la forme factorisée.
		\item Remplir le tableau de signes et de variations de la figure \ref{fig:var-signe}.
		\item Compléter les variations avec les images de $f$ aux bornes de $\D$ ainsi qu'aux extrema locaux.
		\item En déduire le maximum et le minimum de $f$ sur $\D$ et les antécédents qui les réalisent.
	\end{enumerate}
}{}


\begin{figure}[h]
	\centering	
	\begin{tikzpicture}
		\tkzTabInit
		 %[lgt=3,espcl=1.5]
	       		{$x$ / 1 ,  Signe de $2x+1$ / 1, Signe de $3x-2$ / 1, Signe de $f'(x)$ / 1, Variation de $f(x)$ / 2}
	       		{{-1},,,,2}
	\end{tikzpicture}
	\caption{Tableau de signes de $f'$ et de variations de $f$.}
	\label{fig:var-signe}
\end{figure}

\newpage

\exe{
	On souhaite étudier les variations de la fonction
		\[ f(x) = x 2^{-x}, \]
	sur le domaine $\D = [0; 2]$.
	
	Il est admis que la dérivée de $f$ est donnée par
		\[ f'(x) = (1-Kx)2^{-x}, \]
	où $K \approx 0,693147$ est une constante.
	
	\begin{enumerate}
		\item Remplir le tableau de signes et de variations de la figure \ref{fig:var-signe2}. On rappelle que $2^x > 0$ pour tout $x\in\R$.
		\item En déduire le maximum de $f$ sur $\D$ ainsi que l'antécédent qui le réalise.
	\end{enumerate}
}{}

\begin{figure}[h]
	\centering	
	\begin{tikzpicture}
		\tkzTabInit
		 %[lgt=3,espcl=1.5]
	       		{$x$ / 1 , Signe de $1-Kx$/ 1, Signe de $f'(x)$ / 1, Variation de $f(x)$ / 2}
	       		{{0},,,,2}
	\end{tikzpicture}
	\caption{Tableau de signes de $f'$ et de variations de $f$.}
	\label{fig:var-signe2}
\end{figure}


\end{document}